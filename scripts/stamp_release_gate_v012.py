# -*- coding: utf-8 -*-
"""Ворота выпуска ревизии v0.12: два конечных условия, записанные ДО проверок.

Зачем. Whitepaper v0.11 опубликован как отчёт о субстрате. После публикации
получена классификация вопроса об α′β′ (results/v1c/kc_classification_result.json),
и её центральный результат достаточно устойчив, чтобы войти в документ. Вносить
его сейчас нельзя по причине, не связанной с прочностью: две проверки его
интерпретационного окружения открыты, и выпуск при них создал бы ровно то
дробное обновление, от которого проект отказался заранее.

Ворота записаны ДО того, как проверки выполнены, и захэшированы. Условие,
сформулированное после результата, есть выбор того результата, который удобно
считать достаточным.

Существенно: разрешение механизмов M2 и M3 в ворота НЕ входит. Они не являются
предусловием центрального вывода и прямо помечены как не исключённые. Сделать их
условием выпуска значило бы завести бесконечную цепочку «ещё одна проверка».

Запуск:  python scripts/stamp_release_gate_v012.py
"""
from __future__ import annotations

import hashlib
import json
import sys
from pathlib import Path

HERE = Path(__file__).resolve().parent
OUT = HERE.parent / "results" / "v1c"

GATE = {
    "artefact_kind": "ворота выпуска ревизии whitepaper v0.12",
    "date": "2026-09-09",
    "written": "ДО выполнения обеих проверок",
    "decision": "whitepaper сейчас не трогать; выпустить одну консолидированную "
                "ревизию после закрытия ровно двух проверок ниже",

    "why_not_now": "центральный вывод классификации устойчив — он опирается "
                   "только на Inada 2017 и на прогон V1c, — но две проверки его "
                   "интерпретационного окружения открыты. Выпуск при них "
                   "означал бы либо третью ревизию позже, либо сознательное "
                   "оставление свежих результатов вне документа. И то и другое "
                   "есть дробное обновление, от которого проект отказался до "
                   "первого выпуска.",

    "gate_conditions": [
        {"id": "G1",
         "what": "закрыть Figure S3 работы Liu et al. 2022",
         "why": "не потому, что от неё зависит центральный вывод — он от Liu не "
                "зависит вовсе, — а чтобы определить, что именно Liu разрешено "
                "говорить про грибовидное тело",
         "terminating_outcomes": ["FOUND: фигура получена и прочитана",
                                  "SEARCHED-NOT-FOUND: дополнительные материалы "
                                  "недоступны, перечислено что проверено"]},
        {"id": "G2",
         "what": "закрыть поиск численных значений порога генерации спайка для "
                 "αβ, α′β′ и γ",
         "why": "если численные значения существуют, документ их приводит; если "
                "нет — документ сообщает именно это, и отсутствие численной "
                "идентификации становится записанным фактом, а не умолчанием",
         "terminating_outcomes": ["FOUND: значения с источником",
                                  "SEARCHED-NOT-FOUND: перечислено что "
                                  "проверено и почему не подошло"]},
    ],

    "explicitly_NOT_in_the_gate": {
        "what": "разрешение механизмов M2 и M3",
        "why": "они не предусловие центрального вывода и прямо помечены как не "
               "исключённые. Условие выпуска, зависящее от них, порождает "
               "бесконечную цепочку дополнительных проверок — способ, которым "
               "законченные проекты чаще всего не заканчиваются",
    },

    "what_v012_will_say": {
        "structure_of_section_5_10": "три уровня: наблюдение V1c → внешняя "
                                     "физиологическая проверка → граница вывода",
        "chain": ["V1c: KCα′β′ почти не рекрутируются, и два выхода, питаемых "
                  "только от них, молчат полностью",
                  "Inada 2017: порог генерации спайка у α′β′ ниже, чем у αβ и γ, "
                  "при статистически неразличимом соматическом входном "
                  "сопротивлении",
                  "модель [2]: порог у всех трёх подтипов один",
                  "следовательно: допущение о едином пороге как биологически "
                  "верном отображении фальсифицировано"],
        "mandatory_limiter": "направление ошибки установлено — величина поправки "
                             "НЕТ. Следовательно новое семейство не "
                             "авторизовано, и четвёртая ручка не вводится.",
        "why_this_is_the_right_ending": "исход V1c перестаёт быть просто "
                                        "отрицательным «семейство не работает» "
                                        "и становится диагностическим: найдено "
                                        "конкретное биологически неверное "
                                        "допущение отображения, и при этом "
                                        "данных недостаточно, чтобы независимо "
                                        "специфицировать его замену. Читателю, "
                                        "который собирается строить на этом "
                                        "субстрате, полезно именно это.",
    },

    "declared_by": "владелец методологических решений, 9 сентября 2026",
}


def main() -> int:
    p = OUT / "release_gate_v012.json"
    h = OUT / "release_gate_v012_sha256.txt"
    if p.exists():
        print("ворота уже записаны: %s\nперезапись запрещена: условия объявлены "
              "до проверок, и переписать их после значило бы выбрать условие "
              "под результат." % p, file=sys.stderr)
        return 1
    OUT.mkdir(parents=True, exist_ok=True)
    p.write_text(json.dumps(GATE, ensure_ascii=False, indent=2), encoding="utf-8")
    digest = hashlib.sha256(p.read_bytes()).hexdigest()
    h.write_text(
        "# Ворота выпуска ревизии whitepaper v0.12. Записаны ДО выполнения\n"
        "# обеих проверок: условие, сформулированное после результата, есть\n"
        "# выбор того результата, который удобно считать достаточным.\n#\n"
        "# Дата записи: 2026-09-09\n"
        "release_gate_v012.json %d %s\n" % (p.stat().st_size, digest),
        encoding="utf-8")
    print("ворота записаны: %s\nхэш: %s\n" % (p, digest[:16]))
    for c in GATE["gate_conditions"]:
        print("  %s: %s" % (c["id"], c["what"]))
        for o in c["terminating_outcomes"]:
            print("       завершает: %s" % o)
    print("\nНЕ входит в ворота: %s — %s"
          % (GATE["explicitly_NOT_in_the_gate"]["what"],
             GATE["explicitly_NOT_in_the_gate"]["why"]))
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
